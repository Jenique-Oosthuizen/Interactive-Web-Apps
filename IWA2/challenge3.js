/*
 It is important to show the following message in order to;
  - assure users
  - scare hackers
  - impress investors
*/
var warnMessage = 'Security scan starting';
console.warn(warnMessage);

/* It is important to let user know when they can close the page  */

var infoMessage = 'Please wait for scan to complete before closing the browser.';
console.info(infoMessage);